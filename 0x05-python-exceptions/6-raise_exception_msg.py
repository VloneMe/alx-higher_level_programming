def raise_exception_msg(message=""):
    """
    Raises a NameError exception with a custom error message.

    Raises:
        NameError: Always raises a NameError with the specified error message.
    """
    raise NameError(message)
